# =============================================================================
# 비즈니스 관리 시스템 로그인 앱 URL 설정
# =============================================================================
# 설명: 사용자 인증과 관련된 URL 패턴을 정의
# 작성자: 비즈니스 관리 시스템 개발팀
# 버전: 1.0.0
# =============================================================================

# Django URL 패턴 함수 임포트
from django.urls import path
# 현재 앱의 뷰 함수 임포트
from . import views

# =============================================================================
# URL 패턴 정의
# =============================================================================
# 각 URL 경로에 해당하는 뷰 함수를 매핑
urlpatterns = [
    # 로그인 페이지
    # URL: /login/ 또는 /login
    # 뷰 함수: views.login_page
    # 이름: 'login' (템플릿에서 URL 역참에 사용)
    path('', views.login_page, name='login'),
]
